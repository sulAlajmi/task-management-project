# MongoDB connection (default: mongodb://127.0.0.1:27017/task-management)
# For local MongoDB via Docker: docker compose up -d
MONGODB_URI=mongodb://127.0.0.1:27017/task-management

# JWT secret for signing/verifying tokens (set a strong value in production)
JWT_SECRET=your-secret-key-change-in-production

# Server port (default: 3000)
PORT=3000
